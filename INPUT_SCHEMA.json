{
  "title": "Scraper input",
  "description": "No description",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "year": {
      "title": "Year",
      "type": "string",
      "description": "A year of the QS rankings",
      "default": "2020",
      "editor": "select",
      "enum": ["2017", "2018", "2019", "2020"]
    }
  },
  "required": ["year"]
}
